"use client";import {QRCodeCanvas} from "qrcode.react";import {APP_URL} from "@/lib/config";export function QrPanel(){function download(){const c=document.querySelector("canvas");if(!c)return;const a=document.createElement("a");a.download="nieto-green-care-qr.png";a.href=c.toDataURL("image/png");a.click()}return <div className="card" style={{maxWidth:500}}><QRCodeCanvas value={APP_URL} size={320} level="H" marginSize={4}/><p>{APP_URL}</p><button onClick={download} className="btn btn-primary">Download PNG</button></div>}
