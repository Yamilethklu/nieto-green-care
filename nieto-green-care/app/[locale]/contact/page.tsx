import {notFound} from "next/navigation";import Link from "next/link";import {isLocale} from "@/lib/i18n";import {BUSINESS} from "@/lib/config";import {ContactForm} from "@/components/ContactForm";export default async function Page({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();const es=locale==="es";return <section className="section"><div className="container grid" style={{gridTemplateColumns:"repeat(auto-fit,minmax(280px,1fr))"}}><div><div className="eyebrow">{es?"Contacto":"Contact"}</div><h1 className="title">{es?"Hablemos de su propiedad":"Let’s talk about your property"}</h1><p><strong>Nieto Green Care</strong></p><p><a href={`tel:${BUSINESS.phone}`}>{BUSINESS.displayPhone}</a><br/><a href={`mailto:${BUSINESS.email}`}>{BUSINESS.email}</a></p><div className="actions"><a className="btn btn-primary" href={`tel:${BUSINESS.phone}`}>Call</a><a className="btn btn-light" href={`sms:${BUSINESS.phone}`}>Text</a><a className="btn btn-light" href={`mailto:${BUSINESS.email}`}>Email</a><Link className="btn btn-accent" href={`/${locale}/quote`}>Get Quote</Link></div></div><div className="card"><ContactForm/></div></div></section>}
