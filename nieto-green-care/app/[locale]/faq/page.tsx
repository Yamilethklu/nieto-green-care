import {notFound} from "next/navigation";import {isLocale} from "@/lib/i18n";import {faqs} from "@/lib/faqs";import {JsonLd} from "@/components/JsonLd";export default async function Page({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();const visible=faqs.map(f=>f[locale]);return <section className="section"><JsonLd data={{"@context":"https://schema.org","@type":"FAQPage",mainEntity:visible.map(([q,a])=>({"@type":"Question",name:q,acceptedAnswer:{"@type":"Answer",text:a}}))}}/><div className="container prose"><div className="eyebrow">FAQ</div><h1 className="title">{locale==="es"?"Respuestas claras antes de solicitar":"Clear answers before you request service"}</h1>{visible.map(([q,a])=><details className="card" key={q}><summary><strong>{q}</strong></summary><p>{a}</p></details>)}</div></section>}
