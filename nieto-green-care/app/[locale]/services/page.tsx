import {getServices} from "@/lib/services";import {isLocale} from "@/lib/i18n";import {notFound} from "next/navigation";import {ServiceCards} from "@/components/ServiceCards";export const metadata={title:"Lawn & Landscaping Services"};export default async function Page({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();return <section className="section"><div className="container"><div className="eyebrow">{locale==="es"?"Catálogo":"Service catalog"}</div><h1 className="title">{locale==="es"?"Servicios para cuidar su exterior":"Outdoor services, clearly explained"}</h1><p className="muted">{locale==="es"?"Seleccione un servicio para conocer el proceso y solicitar una cotización.":"Choose a service to understand the process and request a property-specific quote."}</p><ServiceCards services={await getServices()} locale={locale}/></div></section>}
