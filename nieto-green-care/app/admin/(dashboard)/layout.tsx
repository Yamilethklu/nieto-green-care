import Link from "next/link";import {redirect} from "next/navigation";import {requireAdmin} from "@/lib/supabase/auth";import {Logout} from "@/components/admin/Logout";export default async function Layout({children}:{children:React.ReactNode}){const auth=await requireAdmin();if(!auth)redirect("/admin/login");const nav=[["","Overview"],["leads","Leads"],["services","Services"],["pricing","Pricing"],["service-areas","Service Areas"],["gallery","Gallery"],["customers","Customers"],["qr","QR Code"],["settings","Settings"]];return <div className="admin-shell"><aside className="sidebar"><strong>NIETO GREEN CARE</strong>{nav.map(([x,n])=><Link href={`/admin/${x}`} key={x}>{n}</Link>)}<Logout/></aside><main className="admin-main">{children}</main></div>}
