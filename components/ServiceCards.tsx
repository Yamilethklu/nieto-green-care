import Link from "next/link";import type {Locale,Service} from "@/types";import {localized} from "@/lib/data";import {t} from "@/lib/i18n";export function ServiceCards({services,locale}:{services:Service[];locale:Locale}){return <div className="grid cards">{services.map(s=>{const l=localized(s,locale);return <article className="card" key={s.id}><div className="eyebrow">{String(s.sort_order).padStart(2,"0")}</div><h3>{l.name}</h3><p className="muted">{l.description}</p><strong>{s.starting_price>0?`${locale==="es"?"Desde":"Starting at"} $${s.starting_price}`:t[locale].custom}</strong><p><Link href={`/${locale}/services/${s.slug}`}>Learn more →</Link></p></article>})}</div>}
