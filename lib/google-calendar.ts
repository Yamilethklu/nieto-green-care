import {createSign} from "node:crypto";

type CalendarLead={id:string;full_name:string;property_address:string;requested_date:string;requested_time:string|null;phone:string;email:string;notes:string|null;estimated_price:number|null;frequency:string};
const b64=(value:string|Buffer)=>Buffer.from(value).toString("base64url");
async function accessToken(){const email=process.env.GOOGLE_SERVICE_ACCOUNT_EMAIL;const rawKey=process.env.GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY;if(!email||!rawKey)return null;const key=rawKey.replace(/\\n/g,"\n");const now=Math.floor(Date.now()/1000);const header=b64(JSON.stringify({alg:"RS256",typ:"JWT"}));const claim=b64(JSON.stringify({iss:email,scope:"https://www.googleapis.com/auth/calendar",aud:"https://oauth2.googleapis.com/token",iat:now,exp:now+3600}));const unsigned=`${header}.${claim}`;const signer=createSign("RSA-SHA256");signer.update(unsigned);signer.end();const jwt=`${unsigned}.${signer.sign(key).toString("base64url")}`;const r=await fetch("https://oauth2.googleapis.com/token",{method:"POST",headers:{"content-type":"application/x-www-form-urlencoded"},body:new URLSearchParams({grant_type:"urn:ietf:params:oauth:grant-type:jwt-bearer",assertion:jwt}),cache:"no-store"});if(!r.ok)throw new Error("Google Calendar authentication failed");const body=await r.json() as {access_token:string};return body.access_token;}
export async function createCalendarEvent(lead:CalendarLead){const calendarId=process.env.GOOGLE_CALENDAR_ID;if(!calendarId)return null;const token=await accessToken();if(!token)return null;const time=(lead.requested_time||"09:00").slice(0,5);const start=`${lead.requested_date}T${time}:00`;const endDate=new Date(`${lead.requested_date}T${time}:00`);endDate.setMinutes(endDate.getMinutes()+60);const end=`${lead.requested_date}T${String(endDate.getHours()).padStart(2,"0")}:${String(endDate.getMinutes()).padStart(2,"0")}:00`;const event={summary:`Nieto Green Care — ${lead.full_name}`,location:lead.property_address,description:`Customer: ${lead.full_name}\nPhone: ${lead.phone}\nEmail: ${lead.email}\nFrequency: ${lead.frequency}\nEstimate: ${lead.estimated_price??"Custom quote"}\n${lead.notes||""}`,start:{dateTime:start,timeZone:"America/Chicago"},end:{dateTime:end,timeZone:"America/Chicago"},extendedProperties:{private:{lead_id:lead.id}}};const r=await fetch(`https://www.googleapis.com/calendar/v3/calendars/${encodeURIComponent(calendarId)}/events`,{method:"POST",headers:{authorization:`Bearer ${token}`,"content-type":"application/json"},body:JSON.stringify(event),cache:"no-store"});if(!r.ok)throw new Error(`Google Calendar event creation failed (${r.status})`);const body=await r.json() as {id:string};return body.id;}
