import {notFound} from "next/navigation";import {isLocale} from "@/lib/i18n";import {getServices} from "@/lib/services";import {QuoteWizard} from "@/components/quote/QuoteWizard";export const metadata={title:"Measure Your Lawn & Request a Quote"};export default async function Page({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();return <section className="section soft"><div className="container" style={{maxWidth:900}}><div className="eyebrow">{locale==="es"?"Cotizador":"Property quote"}</div><h1 className="title">{locale==="es"?"Cuéntenos sobre su propiedad":"Tell us about your property"}</h1><p className="muted">{locale==="es"?"Mida el área de césped y envíe una solicitud. Todas las fechas y estimaciones están sujetas a revisión.":"Measure the lawn and send a request. Dates and estimates are subject to review."}</p><QuoteWizard locale={locale} services={await getServices()}/></div></section>}
