import Link from "next/link";import {areas} from "@/lib/data";import {isLocale} from "@/lib/i18n";import {notFound} from "next/navigation";export default async function Page({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();return <section className="section"><div className="container"><div className="eyebrow">Central Texas</div><h1 className="title">{locale==="es"?"Áreas de servicio confirmadas":"Confirmed service areas"}</h1><div className="grid cards">{areas.map(a=><Link className="card" href={`/${locale}/service-areas/${a.slug}`} key={a.slug}><h2>{a.name}, TX</h2><p className="muted">{locale==="es"?a.es:a.en}</p><strong>{locale==="es"?"Ver detalles":"Explore local service"} →</strong></Link>)}</div></div></section>}
